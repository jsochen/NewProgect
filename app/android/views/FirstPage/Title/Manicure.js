'use struct';
import React,
    {
    AppRegistry,
    StyleSheet,
    Component,
    Text,
    View,
    }from 'react-native';
export default class Manicure extends Component {
    render() {
        return (
            <View>Manicure</View>
        );
    }
}
