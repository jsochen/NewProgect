'use struct';
import React,
    {
    AppRegistry,
    StyleSheet,
    Component,
    Text,
    View,
    }from 'react-native';
export default class Takeaway extends Component {
    render() {
        return (
            <View>Takeaway</View>
        );
    }
}
