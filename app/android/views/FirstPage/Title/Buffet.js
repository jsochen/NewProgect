'use struct';
import React,
    {
    AppRegistry,
    StyleSheet,
    Component,
    Text,
    View,
    }from 'react-native';
export default class Buffet extends Component {
    render() {
        return (
            <View>Buffet</View>
        );
    }
}
