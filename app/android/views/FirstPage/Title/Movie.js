'use struct';
import React,
    {
    AppRegistry,
    StyleSheet,
    Component,
    Text,
    View,
    }from 'react-native';
export default class Movie extends Component {
    render() {
        return (
            <View>Movie</View>
        );
    }
}
